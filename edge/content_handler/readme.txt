# Edge Content Handler

Edge Content Handler works as a Kafka consumer which receives "add" and "remove" caching messages. A KAFKA_BROKER environment variable must be set in edge_node_deployment.yaml to point to the Kafka broker that you wish to use.
